name: Build scummvm.mini

on:
  workflow_dispatch:
    inputs:
      scummvm_version:
        description: 'ScummVM version tag'
        default: 'v2026.1.0'
  workflow_call:
    inputs:
      scummvm_version:
        type: string
        default: 'v2026.1.0'

permissions:
  contents: write
  packages: write

env:
  IMAGE: ghcr.io/spruceui/scummvm-builder-mini

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Ensure beta release exists
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="beta-${{ github.ref_name }}"
          gh release view "$TAG" >/dev/null 2>&1 || \
            gh release create "$TAG" \
              --target "${{ github.ref_name }}" \
              --title "Beta Build (${{ github.ref_name }})" \
              --prerelease \
              --notes "ScummVM binaries built from ${{ github.ref_name }}" \
            || true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.mini
          push: true
          tags: ${{ env.IMAGE }}:latest
          cache-from: type=registry,ref=${{ env.IMAGE }}:cache
          cache-to: type=registry,ref=${{ env.IMAGE }}:cache,mode=max

      - name: Restore ccache
        uses: actions/cache@v4
        with:
          path: ccache
          key: ccache-mini-${{ github.event.inputs.scummvm_version || 'v2026.1.0' }}-${{ github.sha }}
          restore-keys: |
            ccache-mini-${{ github.event.inputs.scummvm_version || 'v2026.1.0' }}-
            ccache-mini-

      - name: Build ScummVM
        run: |
          mkdir -p output ccache
          docker run --rm \
            -e SCUMMVM_VERSION=${{ github.event.inputs.scummvm_version || 'v2026.1.0' }} \
            -v ${{ github.workspace }}/output:/output \
            -v ${{ github.workspace }}/ccache:/ccache \
            ${{ env.IMAGE }}:latest

      - name: Upload build
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          mv output/scummvm output/scummvm.mini
          gh release upload "beta-${{ github.ref_name }}" output/scummvm.mini --clobber
          cd output
          tar czf scummvm-mini-data.tar.gz plugins/ Theme/ Extra/
          gh release upload "beta-${{ github.ref_name }}" scummvm-mini-data.tar.gz --clobber
          cd libs
          tar czf ../scummvm-mini-libs.tar.gz *.so*
          gh release upload "beta-${{ github.ref_name }}" ../scummvm-mini-libs.tar.gz --clobber
